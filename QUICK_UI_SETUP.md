# UI 版本快速设置

## 🎨 这个版本有什么？

- ✅ **悬浮按钮** - 屏幕上一个可爱的红色圆形按钮（🧹）
- ✅ **可拖动** - 按钮可以拖到屏幕任意位置
- ✅ **完整UI菜单** - 点击按钮弹出功能菜单
- ✅ **所有功能** - 清理、变更标识符、重置、查看状态

---

## 🚀 使用步骤

### 1. 重命名文件

把 `Tweak_WithUI.xm` 重命名为 `Tweak.xm`：

```bash
# 在云 Mac 上执行
cd ios-xhs-plugin
mv Tweak_WithUI.xm Tweak.xm
```

### 2. 编译

```bash
bash compile_on_mac.sh
```

### 3. 注入安装

- 拿到 `XHSPlugin.dylib`
- 用你的注入软件注入到小红书 IPA
- TrollStore 安装到手机

---

## 📱 手机上的使用

1. **打开小红书**
2. **等2秒钟** - 会看到一个红色的悬浮按钮 🧹
3. **点击按钮** - 弹出功能菜单
4. **选择你要用的功能**

### 按钮可以拖动！

按住红色按钮可以拖到屏幕任意位置，不会挡视线。

---

## 🎯 菜单功能

| 功能 | 说明 |
|------|------|
| 🔑 清理 Keychain | 只清理 Keychain |
| 🆔 变更标识符 | 输入新的 ID 并变更 |
| ♻️ 完整清理并重置 | 清理所有 + 换新 ID |
| 📊 查看当前状态 | 显示现在的 ID 和 Keychain 数量 |

---

## ⚠️ 如果没看到悬浮按钮

1. 杀了小红书重新打开
2. 再等几秒
3. 查看设备日志有没有 `[XHSPlugin]` 的输出

---

就这么简单！祝你使用愉快！🎉
